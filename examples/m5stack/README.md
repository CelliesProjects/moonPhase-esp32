M5Stack MoonPhase example - How to use

Copy the folder `images` to a fat32 formatted SD card.

Ajust `WIFI_SSID`, `WIFI_PASS` and `TZ_INFO` in `main.cpp`.  
Insert the SD card in the device and flash the sketch to the device.
